# 04 数据库设计

> **版本**: v1.0 | **日期**: 2026-08-23 | **适用对象**: 后端 / DBA
> **权威声明**: 本文件是全部存储对象的**命名权威定义源**（Qdrant Collection、Redis Key、Neo4j 标签/关系/约束、ES 索引）。其余文档只引用不另行命名。payload 业务键规范与架构文档 §3.2 同步维护。
> **上游依据**: 架构文档 P6/P7、第 9 层记忆层、D9 部署拓扑。

---

## 1. 存储职责总览

| 存储 | 版本 | 职责 | 权威性 |
|------|------|------|--------|
| Postgres | 16.x | LangGraph thread checkpoint（会话线程状态权威源，J21） | 会话状态的 SSOT |
| Qdrant | ≥1.10 | 向量检索（dense+sparse）、情景记忆、L1 语义缓存、社区摘要向量 | 向量数据 SSOT |
| Neo4j | 5.x community | 图谱数据（实体/关系/社区） | 图谱 SSOT（J6） |
| Elasticsearch | 8.x + IK | 全文索引（实体+chunk 正文），由 Neo4j/管道异步同步（J5/J6） | 派生索引，允许秒级滞后 |
| Redis | 7.x AOF | 工作记忆、用户画像、L2 检索缓存、限流计数器 | 可丢失重建（工作记忆除外，AOF 保护） |

## 2. Postgres —— LangGraph Checkpoint

### 2.1 表结构

表由 langgraph-server 自动创建与迁移（`langgraph.json` 指定 postgres 连接），**业务代码禁止手写这些表**：

| 表 | 内容 |
|----|------|
| `threads` | 线程元数据（thread_id, metadata, created_at, updated_at） |
| `runs` | 运行记录（run_id, thread_id, status, metadata）——支撑 /sessions/{id}/messages 聚合 |
| `checkpoints` | 图状态快照（含 AgentState 全字段，J21） |
| `checkpoint_blobs` / `checkpoint_writes` | 大值外置与待写入队列 |

### 2.2 DBA 关注点

| 项 | 定案 |
|----|------|
| 备份 | 每日 `pg_dump -Fc` 全量 + WAL 归档（保留 7 天 PITR） |
| 保留策略 | checkpoints 默认全量保留；≥90 天的 checkpoint 由 server 的 TTL 配置清理，threads 元数据永久 |
| 容量估算 | 单次 run 约 3-6 个 checkpoint × 平均 50KB ≈ 0.3MB；1 万次问答 ≈ 3GB，按月监控 |
| 连接 | `POSTGRES_DSN=postgresql://rag:***@postgres:5432/rag`；连接池上限见 server 配置 |

### 2.3 管道元数据表 `doc_documents`（P1 处理清单 SSOT）

业务侧唯一自建 Postgres 表——采集层「已处理清单」的落库位置（架构 P1；关联单元 01 §6.2 单元 1.1）：

| 列 | 类型 | 约束 | 说明 |
|----|------|------|------|
| doc_id | uuid | PK | 与 RawDocument.doc_id 一致（架构 §3.1） |
| source_path | text | NOT NULL | 来源路径或 URL |
| content_hash | char(64) | UNIQUE NOT NULL | SHA-256；增量扫描与去重判断依据 |
| mime_type | text | | 原始 MIME |
| status | text | CHECK IN `pending/parsed/cleaned/chunked/indexed/failed` | 管道阶段水位（P1→P6 推进） |
| quality_score | real | CHECK (0≤x≤1) | P3 门控产出回写 |
| ingested_at / updated_at | timestamptz | NOT NULL DEFAULT now() | |

- 写入方式：采集器入库事务内 `INSERT ... ON CONFLICT (content_hash) DO NOTHING`，受影响行数=0 即判定为重复并跳过
- `status` 由各管道阶段顺序推进，失败停在原水位并记 `failed` 原因列（复用 updated_at 审计）
- 文档删除联动：作为级联起点（见 §7.2），先删本表行再触发各存储清理

## 3. Qdrant Collections

**命名规范**：`rag_<用途>`。业务集合按 doc_type 一型一集合。

### 3.1 业务向量集合：`rag_recipes` / `rag_tips` / `rag_knowledge` …（P6）

```http
PUT /collections/rag_recipes
{
  "vectors": { "dense": { "size": 1024, "distance": "Cosine" } },
  "sparse_vectors": { "sparse": { "index": { "on_disk": false } } },
  "hnsw_config": { "m": 16, "ef_construct": 200 }
}
```

| 项 | 值 | 说明 |
|----|-----|------|
| dense | BGE-M3 输出，1024 维 Cosine | 语义相似性（L3 密集检索） |
| sparse | BGE-M3 sparse，named vector `sparse`，Dot Product | 关键词精确匹配（L3 稀疏检索）；需 Qdrant ≥1.10 |
| 写入 | batch_size=100，async（P6） | upsert 幂等键 = point id |

**Point ID 与 payload 规范**：

- Point ID = `chunk_id`（即 `{doc_id}-{seq}`，与 Neo4j :Chunk.chunk_id、ES `_id` 三方一致）
- payload 键集 = 架构 §3.2 统一 metadata 键规范（doc_id/chunk_id/source/doc_type 必填；category/title_path/created_at/quality_score/lang 可选）＋检索增强键：

| 键 | 类型 | 说明 |
|----|------|------|
| content | string | chunk 正文（召回后免二次查询） |
| keywords | string[] | P5 关键词 |
| access_count | int | J15 访问计数，检索命中时原子自增 |
| community_id / level | string/int | 仅社区摘要 point（source="global"，P7 G5） |

### 3.2 情景记忆集合：`rag_episodic`（J17）

```json
{ "vectors": { "dense": { "size": 1024, "distance": "Cosine" } }, "hnsw_config": { "m": 16, "ef_construct": 200 } }
```

| 字段 | 说明 |
|------|------|
| id | `{session_id}-{turn_seq}` |
| payload | `session_id`(keyword) · `user_id`(keyword) · `timestamp`(int) · `question` · `answer` · `summary`（话题段摘要） |

写入时机：图内尾节点每轮 QA 入库一次；删除会话时按 session_id 过滤清除。

### 3.3 L1 语义缓存集合：`rag_cache`（H2）

```json
{ "vectors": { "dense": { "size": 1024, "distance": "Cosine" } } }
```

| 字段 | 说明 |
|------|------|
| vector | 规范化问题的 embedding（改写前原始查询） |
| payload | `question` · `answer` · `citations_json` · `matched_doc_ids[]` · `latency_tier` · `created_at`(unix) · `model` |

命中规则：top-1 ANN 且 `score ≥ 0.95`（precheck 实现）。**Qdrant 无原生 TTL**——过期策略为应用层定时任务：每日清理 `created_at < now-1h` 的点（L1 TTL，第 9 层）；`doc_id` 反查失效联动见 §7。

## 4. Redis Key 规范

统一前缀小写冒号分层；本表为唯一命名出处：

| Key 模式 | 类型 | 内容 | TTL | 用途 |
|----------|------|------|-----|------|
| `wm:{session_id}` | List | 最近 N 轮 JSON：`{"q":"...","a":"...","ts":1755930000}` | 7d（滑动窗口裁剪由应用层 LPUSH+LTRIM 维护） | 工作记忆（J17，注入在改写前） |
| `user:{user_id}:profile` | Hash | preferences · summary · updated_at | 无（随用户生命周期） | 长期画像，由情景记忆定期蒸馏更新 |
| `l2:ret:{norm_hash}` | String(JSON) | 检索结果 RetrievalResult[] 序列化 | 600s（10min，L2 短 TTL） | 检索缓存，norm_hash = sha256(规范化查询+参数) 前 24 位 |
| `rl:{principal}:{yyyymmddHHMM}` | String(counter) | 请求数 | 120s | 令牌桶辅助计数（principal = user_id 或 api_key 前缀） |

**实例配置**：
- `appendonly yes` + `appendfsync everysec`（工作记忆不丢）
- `maxmemory-policy volatile-lru`（仅驱逐带 TTL 的缓存类 Key，保护 wm:/user: 无 TTL 数据）

## 5. Neo4j 图模型（J12 混合式 Schema）

### 5.1 节点标签与属性

| 标签 | 关键属性 | 说明 |
|------|----------|------|
| `:Entity` | `canonical_name`（唯一）、`name`、`type`、`description`、`embedding_id?`、`zone: "core"\|"open"`、`status: "approved"\|"pending"`、`freq` | 所有实体的通用标签 |
| `:Entity:Dish` / `:Ingredient` / `:Step` / `:Technique` … | 同上 | 白名单核心域附加标签（G1 白名单枚举，Cypher 固定模板使用具体标签匹配） |
| `:Chunk` | `chunk_id`（唯一）、`doc_id` | 管道 chunk 节点 |
| `:Community` | `community_id`（唯一）、`level`、`summary`、`parent_id?` | Leiden 分层社区（G5） |

> 双标签设计理由：白名单模板用 `(:Dish)-[:REQUIRES]->(:Ingredient)` 精确锁定；开放区实体仅挂 `:Entity`（type="Other"），升级白名单时补加域标签并重放关联三元组（J12 升级机制）。

### 5.2 关系类型

| 关系 | 方向 | 来源 |
|------|------|------|
| `[:MENTIONS {evidence_span}]` | (:Chunk) → (:Entity) | G4，支撑 Local Search 子图扩展与反查溯源 |
| `[:REQUIRES]` `[:HAS_STEP]` `[:APPLIES_TO]` … | 域节点间 | G1 白名单枚举（config/graph_schema.yaml 为准） |
| `[:REL {weight}]` | (:Entity) → (:Entity) | 开放区泛化关系 |
| `[:IN_COMMUNITY]` | (:Entity) → (:Community) | G5 |
| `[:PART_OF]` | (:Community) → (:Community) | 社区层级树（level 递增向上） |

### 5.3 约束与索引（DBA 执行清单）

```cypher
CREATE CONSTRAINT entity_canonical IF NOT EXISTS
FOR (e:Entity) REQUIRE e.canonical_name IS UNIQUE;
CREATE CONSTRAINT chunk_id IF NOT EXISTS
FOR (c:Chunk) REQUIRE c.chunk_id IS UNIQUE;
CREATE CONSTRAINT community_id IF NOT EXISTS
FOR (m:Community) REQUIRE m.community_id IS UNIQUE;

CREATE INDEX entity_type IF NOT EXISTS FOR (e:Entity) ON (e.type);
CREATE INDEX entity_zone_status IF NOT EXISTS FOR (e:Entity) ON (e.zone, e.status);
CREATE INDEX chunk_docid IF NOT EXISTS FOR (c:Chunk) ON (c.doc_id);
CREATE INDEX community_level IF NOT EXISTS FOR (m:Community) ON (m.level);
```

### 5.4 Cypher 模板库

```cypher
-- G4 幂等写入（管道侧）
MERGE (e:Entity {canonical_name: $name})
  SET e.type = coalesce(e.type, $type), e.freq = coalesce(e.freq,0)+1,
      e.zone = coalesce(e.zone,'open'), e.status = coalesce(e.status,'pending')
WITH e MATCH (c:Chunk {chunk_id: $chunk_id})
MERGE (c)-[:MENTIONS {evidence_span: $span}]->(e);

-- Local Search：ES 召回 entity 后一跳邻域扩展（L3 graph_retriever）
MATCH (e:Entity {canonical_name: $entity})-[r]-(n)
RETURN e, r, n LIMIT $limit;

-- Global Search：按 level 取社区摘要（global_retriever 经 Qdrant 召回 community_id 后回查）
MATCH (m:Community {community_id: $cid}) RETURN m.summary;

-- J12 审核升级：approve 后补白名单标签 + 重放关联三元组（admin 接口触发）
MATCH (e:Entity {canonical_name: $name})
SET e.zone='core', e.status='approved'
CALL apoc.create.addLabels(e, [$domain_label]) YIELD node
WITH node MATCH (node)-[r:REL]->(t)
MERGE (node)-[nr:${whitelisted_rel}]->(t);
```

## 6. Elasticsearch 索引设计（J5/J6）

### 6.1 Settings（两索引共用分析器定义）

```json
{
  "settings": {
    "number_of_shards": 1, "number_of_replicas": 0,
    "analysis": {
      "analyzer": {
        "idx_ik_max":   { "type": "custom", "tokenizer": "ik_max_word" },
        "qry_ik_smart": { "type": "custom", "tokenizer": "ik_smart" }
      }
    }
  }
}
```

### 6.2 `rag_entities_v{n}` mapping

| 字段 | 类型 | 分析器 | 说明 |
|------|------|--------|------|
| entity_id | keyword | — | `_id` 同值；回投 Neo4j 的映射键 |
| canonical_name | keyword | — | 精确过滤 |
| name | text | idx_ik_max / qry_ik_smart | 名称全文 |
| aliases | text | 同上 | 别名归并表展开 |
| description | text | 同上 | 描述 |
| type | keyword | — | 实体类型（含 Other） |
| zone | keyword | — | core/open（可选过滤） |

### 6.3 `rag_chunks_v{n}` mapping

| 字段 | 类型 | 分析器 | 说明 |
|------|------|--------|------|
| chunk_id | keyword | — | `_id` 同值；与 Qdrant point id、Neo4j :Chunk 三方一致 |
| doc_id | keyword | — | 父文档 |
| content | text | idx_ik_max / qry_ik_smart | chunk 正文 |
| title_path | text | 同上 | 标题路径拼接 |
| created_at | date | — | ISO 8601 |

### 6.4 别名与重建策略

- 写入走别名 `rag_entities` / `rag_chunks` → 指向 `*_v1`
- 全量重建（J18 类别变更/分词器升级）：建 `*_v2` 灌数 → 校验 count 一致 → 原子切换别名 → 删旧索引，零停机
- 同步链路：graph_writer 成功后 es_syncer 异步 upsert（失败入重试队列，死信人工介入，P6）

## 7. 数据生命周期与失效联动

### 7.1 TTL 总表

| 对象 | 生命周期 | 清理机制 |
|------|----------|----------|
| L1 缓存 point | 1h | 应用层每日清理任务（created_at 过滤 delete） |
| L2 Redis | 10min | 原生 TTL |
| 工作记忆 List | 7d 滑动窗口 | 应用层 LTRIM + TTL 兜底 |
| episodic point | 随用户会话 | 删除会话接口触发按 session_id 过滤删除 |
| checkpoint | 90d | server TTL 配置 |

### 7.2 文档删除联动流程（防缓存答案引用已删内容）

```
DELETE /admin 文档(doc_id)
  ├─ Qdrant: 各 rag_{type} 按 filter{doc_id} 删除 points
  ├─ ES:     delete_by_query {"query":{"term":{"doc_id":"..."}}}
  ├─ Neo4j:  MATCH (c:Chunk {doc_id:$doc}) DETACH DELETE c
  │          （关联 Entity.freq 衰减；freq 归零且无 MENTIONS 入边的开放区实体进回收队列）
  └─ L1/L2:  rag_cache 中 matched_doc_ids 含该 doc_id 的 point 删除;
             l2:ret:* 全量清空（保守策略，成本低）
```

## 8. 容量估算参考（每 1 万 chunks 口径）

| 存储 | 估算 | 依据 |
|------|------|------|
| Qdrant | ~80MB/集合（500字/chunk） | 1024 维 f32 dense + sparse 倒排 + payload |
| ES | ~60MB | 正文倒排 + IK 词项 |
| Neo4j | ~40MB | Chunk/Entity 节点 + MENTIONS 边 |
| Postgres | 见 §2.2 | 随问答量线性 |
| Redis | <100MB | wm/profile/l2 全量 |

单机 compose 默认磁盘配额建议 ≥50GB 并配置各卷监控告警。

---

*变更记录：v1.0（2026-08-23）基于《GraphRAG 系统架构文档 v3.0》P6/P7/J5/J6/J12/J17/H2/D9 创建。*

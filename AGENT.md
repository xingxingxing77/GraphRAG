# AGENT.md —— 编程 Agent 开发入口

> **本文件是编码 Agent 的强制入口**：任何开发会话开始时先完整读取本文件，再按 §3 任务路由加载指定文档，然后才允许动手写代码。
> **项目**: GraphRAG 智能问答系统 | **架构基准**: 《GraphRAG 系统架构文档 v3.0》（编号 J/M/D/H/A/B/E 引用它）

---

## 1. 项目速览

- **双服务架构（J19）**：`FastAPI 业务面 :8000`（认证/会话/反馈/图谱代理/配置/precheck/admin）+ `langgraph-server :8001`（Agent 全链路图，前端 SDK 直连）
- **链路**：查询理解 → 六路并行检索 → RRF 融合 → Reranker 精排 → LangGraph Agent 循环（Planner→ToolRouter→Reflector）→ 缓冲式生成 → 忠实度自校正 → 记忆写回
- **存储五件套**：Postgres(checkpoint) · Qdrant(向量/记忆/缓存) · Neo4j(图谱 SSOT) · Elasticsearch(全文派生) · Redis(工作记忆/L2 缓存)
- **技术栈**：后端 py3.11+/FastAPI/Pydantic v2；前端 React19/TS5/Vite6/Tailwind4/shadcn/Zustand5/pnpm · Beautiful UI（设计系统层，MIT）· recharts · lucide-react

## 2. 文档地图

| 文件 | 内容 | 何时必读 |
|------|------|----------|
| `01_开发流程.md` | 角色/Git/CI/里程碑 DoD/配置变更流程 | 提交前、规划任务时 |
| `02_API接口契约.md` | ★ 全部 HTTP 接口权威定义 + 错误码总表 + TS 类型 | 涉及任何接口的开发 |
| `03_通信协议规范.md` | SSE 帧格式/事件语义/超时重连/降级信号 | 涉及流式、事件、降级传递 |
| `04_数据库设计.md` | ★ 存储命名权威源（Collection/Key/索引/Cypher/mapping） | 涉及数据模型与存储读写 |
| `05_后端开发指南.md` | 编码规约/async 铁律/分层实现范式 | 后端任何开发 |
| `06_前端开发指南.md` | 脚手架/stores/SDK 封装/组件清单/Beautiful UI 设计系统 | 前端任何开发 |
| `前端设计系统落地方案.md` | Beautiful UI 落地计划与方案（J24）：依赖替换/令牌/20 件映射/批次 | 前端设计系统相关开发前 |
| `07_联调与测试计划.md` | 用例矩阵/故障注入/性能验收/golden 门禁 | 写测试、修缺陷、性能工作 |
| `08_文档一致性Lint规范.md` | RAG 文档套件一致性 Lint 规则（R1/R2/R3 文档级 + R7/R8 前端级；R4/R5/R6 代码级待接入） | 文档/CI 一致性审查 |
| `09_Agent派发与契约保障.md` | 派发包模板/检查单/收工自检/契约测试矩阵/联调就绪清单（J25 配套） | 派发任务、收工验收 |
| `10_部署运维Runbook.md` | D9 部署拓扑/环境变量/健康检查/备份恢复/降级 SOP/监控告警 | 运维/SRE 接线与故障处置 |
| `11_数据库与缓存实施路线图.md` | 缓存失效与异步写机制、D4–D9 决策、Qdrant alias 重建、Phase 0–5 实施路线（命名以 04 为准） | 涉及缓存/异步写/存储客户端实现前 |

架构文档不在本目录时，向用户索取路径；本目录文档与架构文档冲突时：**以架构文档为设计权威，以 02/04 为接口与命名的实现权威**，并立即报告冲突。

## 3. 任务路由规则（先读后写，缺一不可）

**任务单携带子阶段编号（x.y）时：先在 `01_开发流程.md` §6 定位该单元行，严格按其「前置阅读」与 §6.0 三步工作流（S1→S2→S3）执行——下表路由与其互补，不替代。**

| 任务类型 | 开始前必须读取 | 完成后必须同步 |
|----------|----------------|----------------|
| 新增/修改 REST 端点 | 02 全文 + 03 §5-§6 + 01 §7(若涉配置) | 02 对应节 + 错误码表 + 06 §9 文案表 + 07 用例矩阵 |
| 修改请求/响应字段 | 架构第三章契约 + 02 §5/§7 | `core/models.py` 与 `types/api.ts` 同 PR + 02 |
| 数据模型/存储变更 | 04 相关节 + 架构 P6/P7 | 04 + 迁移脚本 + 07 失效联动用例核对 |
| Agent 图节点/条件边改动 | 架构第 6 层 + 05 §5.3 + 03 §3.3 | 若新增 updates 语义→03 §3.4 样例更新 |
| 检索器/融合/Rerank 改动 | 05 §5.2 + 架构 L3-L5 | 权重/阈值默认值同步 8.2 配置示例说明 |
| 流式/打字机/降级 UX | 03 全文 + 02 §6 | 03 + 06 §9 |
| 清洗/分块规则改动 | 架构 P3/P4 + 05 §5.6 | 触发 golden set 回归（D8 门禁），J18 判断是否禁热更 |
| 测试/缺陷修复 | 07 对应用例 + 02 断言基准 | 回归通过后在 PR 描述附用例 ID |
| 配置项增删 | 01 §7 + 架构 D7/J18 | pipeline_config/reliability 示例 + 04(若涉存储) |
| BUI 组件新增/修改（设计系统） | 06 §10 全节 + `前端设计系统落地方案.md` + 02 §7(若涉类型) | 06 §10.4 映射核对；banned 依赖/硬编码色由 08 R4 CI 拦截 |

## 4. 硬性约束（违反任意一条 = 实现错误，无论测试是否通过）

1. **契约唯一来源（D1）**：跨层字段只定义在 `app/core/models.py`；前端镜像在 `src/types/api.ts`。禁止层间私加字段、禁止两处各写一份。
2. **缓冲式流（M1）**：standard/deep 档在校验完成前不得向前端流出任何答案文本——中间内容只进 thought 折叠面板。fast 档例外（逐 token 直推）。
3. **降级不抛错（M3/D5）**：所有超限（轮次/token/挂钟预算）与依赖故障走降级作答 + `X-Degraded` 标注；`recursion_limit` 只是最后防线，正常路径不允许触达。
4. **async 三铁律（05 §3.3）**：同步推理必包 executor；GPU 推理过全局 semaphore；外部调用必有独立超时。
5. **循环必有终止预算（M3）**：任何新增回环必须登记 max 次数与超限行为到 M3 表。
6. **命名同源**：端点/错误码只认 02；Collection/Index/Redis Key 只认 04。同名必须同义，新名字先进文档再进代码。
7. **密钥纪律（J16/D7）**：api_key 只经环境变量（api_key_ref），业务策略只进 YAML；禁止明文入代码/日志/前端 bundle。
8. **图谱写入幂等（G4/J12）**：一律 MERGE canonical_name；开放区实体 zone=open，升级走 admin 审核流，禁止代码直改白名单。
9. **决策引用**：讨论/注释/PR 描述中引用决策编号（如 J20/E3），不重新发明论证。
10. **文档即事实**：实现与文档不符时，当天提文档修正 PR——不许"代码将就跑通"。
11. **设计系统约束（J24）**：`src/components/bui/` 仅容纳 Beautiful UI 复制组件；禁止引入 banned 依赖（见 08 R4）；图标统一 `lucide-react`；颜色/阴影/圆角仅经 `globals.css` 令牌。

## 5. 标准开发工作流

**任务属于某个子阶段（编号 x.y，见 01 §6.1-§6.11）时，必须严格遵循 `01_开发流程.md` §6.0 三步工作流执行：S1 后端实现（先读前置阅读）→ S2 前端同步/实现（先读前置阅读，类型镜像必做）→ S3 测试验收。**

```
理解需求 → 按 §3 路由读完指定文档 → 列出影响面(契约/存储/接口/文档)
→ 实现(TDD 优先: 先按 07 断言写测试) → 自检清单
   □ 单测通过        □ golden set 不低于基线(D8)
   □ mypy/tsc 绿     □ LangSmith trace 可回放该链路
   □ §6 同步义务完成 □ Conventional Commits(01 §4.2)
→ 提 PR(关联里程碑任务号)
```

## 6. 变更同步义务速查表

| 你改了… | 必须同步… |
|---------|-----------|
| REST 端点/字段 | `02` + `06/types` + `07` 用例 |
| SSE 事件/帧语义 | `03` + `06 useChatStream` |
| Collection/Index/Key | `04` + `05` 读写代码 + `07` E-05 类用例 |
| Neo4j 白名单 Schema | `config/graph_schema.yaml` + `04 §5` + 审核流程说明 |
| 配置键 | 架构 8.2 示例 + `01 §7`(热更属性判定) |
| 错误码 | `02 §6` 总表 + `06 §9` 文案 |
| M3 预算值 | 架构第 6 层 M3 表 + `reliability.yaml` + `07 §6` 读超时 |

## 7. 常见任务 Playbook

### Playbook A：新增一个检索器
1. 读 05 §3.2/§5.2 → 2. SourceKind 加枚举成员（core/models.py）→ 3. 实现 BaseRetriever 子类（result_id=`{name}:{hash}`、独立超时、失败返回空列表+计数器）→ 4. retrieval/fusion.py 权重表加默认权重并更新配置示例 → 5. 单测(mock 上游)+集成测 → 6. 同步 04(若有新存储结构)。

### Playbook B：新增一个业务面端点
1. 读 02 §2 通用约定 → 2. 在 02 落契约草稿评审 → 3. `app/api/endpoints/` 实现 + Pydantic 模型放 `schemas_api/` → 4. 注册限流/鉴权依赖 → 5. 错误码入 02 §6 表 + 06 §9 文案 → 6. 07 补接口用例 → 7. curl 示例验证。

### Playbook C：给聊天流增加一种事件信息
1. 读 03 §3 全文 → 2. 判定归属：updates 载荷扩展（优先）还是新事件类型（需强理由）→ 3. 后端节点产出 delta 字段 → 4. 03 §3.4 样例补一行 → 5. 前端 summarize() 增分支 → 6. S-01 用例断言补充。

### Playbook D：新增清洗规则
1. 读架构 P3 + J18 → 2. 继承 PipelineRule → 3. YAML 注册 priority → 4. 正反例单测 → 5. 本地跑 golden 回归确认无退化 → 6. 若属可热更类，验证 /admin/config/hot-reload 生效。

### Playbook E：修复一个点踩 bad case
1. 从反馈队列取 case（02 §3.5 注）→ 2. 归因（检索未命中/生成幻觉/引用错位…）→ 3. 按归因走对应 Playbook → 4. 将 case 入 golden set（07 §8 维护流程）→ 5. 回归证明该 case 通过且基线不降。

---

## 8. AI 编码 Agent 派发规范（J25 配套）

本套文档用于「AI 编码 Agent 按文档实现后端」的场景。派发与验收须遵循：

1. **派发原子单位 = 子阶段单元 `x.y`**（01 §6.1–§6.11）。一个单元对应一个独立可验收的 PR 组（≤5 工作日）。
2. **派发包（Context Pack）**：派发前须为该单元生成作用域上下文包，内容严格等于 01 该单元「前置阅读」列出的章节 + 本文件 §4 硬约束 + S1→S2→S3 顺序。Agent 只消费该包，**不丢整库**（防读不全/读错导致跑偏）。生成方式见 `09_Agent派发与契约保障.md` §1。
3. **顺序铁律**：S1 后端自检未过不得进 S2；S3 不通过回退修复重跑（01 §6.0）。
4. **收工自检**：Agent 提 PR 前必跑 `scripts/agent_selfcheck.py`（doc-lint + `mypy --strict` + 本单元 07 用例 + 契约冒烟），全绿方可提交。
5. **文档即事实**：实现与文档冲突 → 先提文档修正 PR（§4.10），不在代码里将就。
6. **决策编号**：代码/PR 引用决策须带编号（§4.9）；新增回环/降级须登记 M3/A/B/E 表。

> 详细模板（派发包模板、派发检查单、契约测试矩阵、联调就绪清单）见 `09_Agent派发与契约保障.md`。

---

*变更记录：v1.0（2026-08-23）随《RAG 开发文档套件》创建；上游《GraphRAG 系统架构文档 v3.0》。v1.1 新增 §8 AI 编码 Agent 派发规范（J25 配套），并联动新建 `09_Agent派发与契约保障.md`。*

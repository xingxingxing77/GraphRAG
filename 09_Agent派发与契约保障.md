# 09 Agent 派发与契约保障

> **版本**: v1.0 | **日期**: 2026-08-23 | **适用对象**: 派发者 / 后端 Agent / 测试
> **定位**: AGENT.md §8 的配套操作手册。定义「派发包模板」「派发检查单」「Agent 收工自检清单」「可执行契约测试矩阵」「联调就绪清单」，把"AI 编码 Agent 按文档实现后端且不跑偏 + 前后端强联调"落为可复用流程。
> **上游依据**: AGENT.md §4/§8、01 §6.0/§6.1–§6.11、02/03/04/06、08（R5/R6）、架构文档 J25。

---

## 1. 派发包（Context Pack）

**定义**：派发一个子阶段单元 `x.y` 时，组装的**最小化作用域上下文**。Agent 只消费此包，不接触整库，从根因上消除"读错章节/读漏约束"导致的跑偏。

**生成**：`scripts/make_agent_pack.py <x.y>`（代码阶段实现）按 01 该单元「前置阅读」的章节引用，从架构/02/03/04/05 抽取拼成 `agent-pack-<x.y>.md`。

**派发包模板**（固定结构）：

```
# Agent Pack · 单元 <x.y> · <单元名>

## 0. 硬约束（必读，来自 AGENT.md §4）
- 契约唯一源 D1 / 缓冲式流 M1 / 降级不抛错 M3-D5 / async 三铁律 / 循环终止预算 M3
- 命名同源 / 密钥纪律 / 图谱幂等 G4 / 决策引用 / 文档即事实

## 1. 本单元职责（来自 01 §6.x）
<单元描述 + 主责角色>

## 2. 前置阅读（来自 01 该单元「前置阅读」）
- 架构文档 §X.X：<要点>
- 02 §Y：<要点>
- 04 §Z：<要点>

## 3. 三步工作流要求（01 §6.0）
- S1 后端：<前置阅读> → 写代码 → 契约改动先落 core/models.py 并同 PR 回写架构第三章
- S2 前端：types/api.ts 由 openapi-typescript 生成（禁手改），见 06 §2
- S3 测试：<07 对应用例> 全过；命中 D8 门禁时跑 golden 回归

## 4. 验收门禁（本单元必过）
- doc-lint 绿 / mypy --strict 绿 / 本单元 07 用例 / 契约测试（08 R5）/ 决策编号引用合规
```

## 2. 派发检查单（派发者 / CI 用）

派发前逐项核对，任一不达标不打 Agent：

- [ ] 单元编号 `x.y` 存在于 01 §6.1–§6.11
- [ ] 该单元「前置阅读」章节在仓库内均可解析（避免引用悬空）
- [ ] 派发包已生成且含 §0 硬约束 + §3 三步要求
- [ ] 若涉及契约变更（02/03/04），已先开文档修正 PR 并评审通过
- [ ] 分支名 `feature/<x.y>-<模块>-<描述>` 已建

## 3. Agent 收工自检清单

Agent 提 PR 前必须全绿（对应 `scripts/agent_selfcheck.py`）：

- [ ] `python scripts/doc_consistency_lint.py .` → 0 硬错误（R1/R2；R5/R6 代码阶段生效后纳入）
- [ ] `mypy --strict app/core app/agent` → 绿
- [ ] `pytest -m unit_<x.y>` → 本单元 07 用例全过
- [ ] 契约冒烟：import app 断言本单元路由/模型存在；OpenAPI 与 02 §3.6 端点对齐
- [ ] 触及分块/清洗/检索/模型时：`tests/golden/run_regression.py` 基线不降（D8）
- [ ] 代码/PR 描述含决策编号引用（如 `J20`/`E3`/`M3`），无"重新发明论证"

## 4. 可执行契约测试矩阵（联调保障，代码阶段落地为 `tests/contract/`）

强联调（J25）把"联调"从人工清单升级为有文档锚点的可执行契约。四类测试断言来源如下：

| 契约测试 | 断言来源（文档锚点） | 失败含义 |
|----------|----------------------|----------|
| `openapi_vs_02` | 02 §3.6 端点 + §5 类型字段 | 后端 OpenAPI 漂离 02 |
| `sse_schema` | 03 §3.3/§3.4 事件序列与载荷（即 07 S-01~S-04） | SSE 流格式漂移 |
| `degraded_parity` | 后端可发 `X-Degraded` ⊆ 02 §2.4，且 06 §9 有对应 Banner 文案 | 降级信号前后端不对齐 |
| `errorcode_parity` | 后端抛出错误码 ⊆ 02 §6，且 06 §9 映射 ⊆ 后端实际发出 | 错误码前后端不对齐 |

> 此四测试为合并门禁（分支保护必跑）；具体实现规格见 08 §2 R5/R6。

## 5. 联调就绪清单（派生自 07 L1–L4）

| 批次 | 前置单元 | 就绪标准（来自 07 §4/§6） |
|------|----------|---------------------------|
| L1 认证与会话 | 10.1+10.2 | 02 §3.1–§3.7 happy path + 401 静默重兑换 |
| L2 聊天主链路 | 10.3+10.4 + 8.1–8.3 | 三档各一条基准对话；thought 折叠 + degraded 角标正确 |
| L3 图谱与闭环 | 2.4 + 10.6 + feedback | NVL 渲染正确；点踩入库可查 |
| L4 管理面 | 10.2 + 9.1 + 7.2 | 热更生效、重建进度轮询、审核队列流转 |

任一批次未达就绪标准，不视为该单元完成（叠加 01 §6.0 全局 DoD）。

## 6. 与代码阶段的关系（后续，非本文档范围）

本文档的"脚本/测试"项在代码阶段实现，清单如下（仅规划，不在此落地）：

- `scripts/make_agent_pack.py`：按 §1 生成派发包
- `scripts/agent_selfcheck.py`：按 §3 跑收工自检
- `tests/contract/`：`openapi_vs_02` / `sse_schema` / `degraded_parity` / `errorcode_parity`
- 前端：`openapi-typescript` 接入 + `pnpm gen:api` / `gen:api --check`
- CI：`.github/workflows/` 增 `contract-test` job；分支保护必跑 doc-lint + 契约测试

---

*变更记录：v1.0（2026-08-23）随 AGENT.md §8 配套创建；落地 J25 强联调治理。*

# 前端设计系统落地方案（Beautiful UI）· 计划与方案 v1

> **版本**: v1.0 | **日期**: 2026-08-23 | **关联决策**: 架构文档 **J24**
> **状态**: 方案已定稿（组件代码与 `globals.css` 落地按 §5 批次执行；本文件为计划存档）
> **来源**: beautifului.dev（MIT，官方展示站，组件源码由浏览器复制）。参考镜像 github.com/ithmz/beautiful-ui（非官方，19 文件组件库）。

---

## 0. 决策摘要（已与用户确认）

| 项 | 决策 |
|---|---|
| 设计系统策略 | Plan A：Beautiful UI 为统一视觉层；shadcn/ui 保留作基础原子层 |
| 组件落点 | `rag-web/src/components/bui/` |
| 暗色模式 | `class="dark"` + `dark:` 变体（已确认与 GraphRAG 兼容） |
| 品牌色 | 保留 BUI 默认 accent（靛紫系），不改 indigo-600 |
| 全局 CSS | 由本方案据 20 组件反推重建（数值初版，需对照官网目视微调） |
| 私有图标 `@central-icons-react/*` | → 映射 `lucide-react` |
| `iconoir-react` | → 映射 `lucide-react` |
| `posthog-js` / `glimm` | 剥离（chat-composer / prompt-bar） |
| `liveline` | → `recharts`（insight-cards 改造） |
| 内部无源码 `Button`/`Shimmer`/`StreamText` | 本地化（shadcn Button / 本地原子 / `useTypewriter`） |
| `GlideMenu` | → shadcn `Popover`/`DropdownMenu` |
| `records-table` | 全量落地 |
| `flowchart` | 全量落地 |

---

## 1. 总体策略

- 复制 Beautiful UI 组件到 `src/components/bui/`，作为"设计系统层"；业务组件仍在 `src/components/` 下**包装** BUI 原子（如 `MessageBubble` 内嵌 `streaming-text`）。
- 不混入 BUI 私有依赖；所有外部/内部依赖按 §2 替换。
- 设计令牌集中在一份重建的 `globals.css`（Tailwind v4 `@theme` + `.dark` 覆盖 + keyframes + 组件专属类）。

---

## 2. 依赖替换总表（实现前必须解决）

| 来源 | 出现组件 | 处理方式 |
|---|---|---|
| `@/components/atoms/Button` | recommendation-card, diff-table | → shadcn `Button` |
| `@/components/atoms/Shimmer` | selection-actions | → 本地 shimmer 原子（CSS `shimmer-text`） |
| `@/components/atoms/StreamText` | selection-actions | → 本地 `StreamText` 原子 / GraphRAG `useTypewriter` |
| `@/components/primitives/GlideMenu` | sidebar-nav×2, records-table, search, fine-tune-card | → shadcn `Popover`/`DropdownMenu` |
| `@central-icons-react/*`（私有） | sidebar-nav | → `lucide-react` 映射表 |
| `iconoir-react` | selection-actions | → `lucide-react` 映射表 |
| `posthog-js` | chat-composer | 剥离（移除 `posthog.capture`） |
| `glimm` | prompt-bar | 剥离（移除 rainbow sweep `createShader`/`playSweep`） |
| `liveline` | insight-cards | → `recharts`（折线/柱状/占比图重写） |

---

## 3. 设计令牌与全局 CSS 重建

**令牌 schema（变量名取自 20 组件；数值为初版反推，需对照 beautifului.dev 目视微调）：**

```css
@import "tailwindcss";
@theme {
  /* ink 文字/图标灰阶 */
  --color-ink:#15171c; --color-ink-2:#5a606b; --color-ink-3:#8b919c;
  /* accent 靛紫系（BUI 默认） */
  --color-accent:#5b5bf0; --color-accent-ink:#5b5bf0;
  --color-accent-tint: color-mix(in srgb,#5b5bf0 12%, var(--color-surface));
  /* 语义色 */
  --color-orange:#f09a2f; --color-green:#25a878; --color-red:#ee5c61;
  --color-green-tint: color-mix(in srgb,#25a878 14%, var(--color-surface));
  --color-red-tint:   color-mix(in srgb,#ee5c61 14%, var(--color-surface));
  /* 表面/结构 */
  --color-surface:#ffffff; --color-canvas:#f7f7f8; --color-page:#f7f7f8;
  --color-field:#f1f1f3; --color-inset:#f4f4f6;
  --color-line:#e7e7ea; --color-line-strong:#d4d4d8;
  --color-hover:#f1f1f3; --color-hover-2:#eaeaf0; --color-white:#fff;
  /* 阴影 */
  --shadow-overlay:0 12px 40px -8px rgba(20,20,40,.18),0 2px 8px rgba(20,20,40,.08);
  --shadow-hairline:0 0 0 1px rgba(20,20,40,.06);
  --shadow-btn:0 1px 2px rgba(20,20,40,.10);
  --shadow-card:0 1px 3px rgba(20,20,40,.08);
  --shadow-raised:0 8px 24px -8px rgba(20,20,40,.18);
  /* 圆角 */
  --radius-control:8px; --radius-card:14px; --radius-chip:9999px;
  /* 字体 */
  --font-sans:"Inter",ui-sans-serif,system-ui,sans-serif;
  --font-mono:"JetBrains Mono",ui-monospace,monospace;
}
/* 自定义时长（组件用到） */
@layer base { .duration-400{transition-duration:400ms} .duration-250{transition-duration:250ms} }

/* 暗色：仅覆盖需反转的令牌 */
.dark { --color-surface:#1c1d22; --color-canvas:#16171b; --color-page:#16171b;
  --color-field:#232429; --color-inset:#202127; --color-line:#2c2d34; --color-line-strong:#3a3b43;
  --color-hover:#232429; --color-hover-2:#2a2b32;
  --color-ink:#ecedf1; --color-ink-2:#a7adb8; --color-ink-3:#6f757f; }

/* keyframes（组件引用全集） */
@keyframes pixel-on{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:none}}
@keyframes shimmer-text{0%{background-position:200% 0}100%{background-position:-200% 0}}
@keyframes pop-in{from{opacity:0;transform:scale(.96)}to{opacity:1;transform:none}}
@keyframes fade-in{from{opacity:0}to{opacity:1}}
@keyframes fade-up{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
@keyframes eq-bounce{0%,100%{transform:scaleY(.4)}50%{transform:scaleY(1)}}
/* spin 为 Tailwind v4 内置，无需定义 */
```

**组件专属类（须定义；`records-*`/`sidebar-*` 先给可用骨架，落地后按视觉收敛）：**

`animated-underline` · `source-avatar` · `primitive-card-pad` · `primitive-card-footer` · `primitive-card-bar` · `primitive-icon-button` · `primitive-table-cell` · `filter-status-todo/progress/done` · `insight-chart-tooltip(-item/-dot)` · `insight-chart-stage` · `insight-chart-cursor` · `insight-chart-tooltip-anchor` · `sidebar-row` · `sidebar-copy` · `sidebar-workspace-control` · `sidebar-collapse-control` · `sidebar-expand-control` · `sidebar-glide-highlight` · `records-*`（约 40+ 类：shell/table/header-cell/cell/tag/checkbox/calc 等）。

---

## 4. 组件 → GraphRAG 映射（20 件）

| 组件 | 落点（业务组件/用途） |
|---|---|
| loading-state | 流式/预检 loader |
| thinking-state | ThoughtPanel（Agent 思考） |
| streaming-text | MessageBubble + TypewriterText + CitationPopover + FeedbackButtons + 追问 |
| approval-card | HITL 人工确认（架构 03 §3.3 预留） |
| tool-chips | AgentStateInspector / FanoutStatusView |
| task-rows | AgentStateInspector / TaskProgressPanel / PlanStepsList |
| chat-composer | ChatPanel 输入/气泡 |
| prompt-bar | ChatPanel 高级输入（TierSelector / ModelPicker） |
| recommendation-card | 建议/追问卡片 |
| context-cards | CitationPopover / 检索块 |
| diff-table | CleaningDiffCard |
| records-table | ScanResultTable / ReviewQueuePanel（全量） |
| filter-table | 筛选 chips（FilterChips） |
| sidebar-nav | 主导航骨架 |
| search | 命令面板/全局搜索 |
| flowchart | 工作流编排画布（全量） |
| insight-cards | 数据洞察/指标卡片（recharts 改造） |
| code-block | 代码块/检视 |
| fine-tune-card | 属性检视器 |
| selection-actions | 选中文本 AI 操作条 |

---

## 5. 落地批次与步骤（退出方案评审后执行）

1. **写 `globals.css`**：令牌 + `.dark` 覆盖 + keyframes + 组件专属类（§3）。
2. **建 `src/components/bui/`**，按依赖序：
   - 批次1（零依赖）：loading / thinking / streaming / code-block / fine-tune / context-cards / tool-chips / task-rows / diff-table
   - 批次2（GlideMenu→shadcn Popover）：sidebar-nav / search / records-table（全量）/ fine-tune-card 菜单
   - 批次3（剥离 posthog/glimm）：chat-composer / prompt-bar
   - 批次4（recharts）：insight-cards
   - 批次5（重/全量）：flowchart（全量）、selection-actions（图标映射 lucide + StreamText/Shimmer 本地化）
3. **执依赖替换**（§2 映射表）。
4. **业务包装**：接 `chatStore` / `sessionStore` / `types/api.ts`（与 `02_API接口契约.md` §7 对齐）。
5. **文档改写**（§7）。

---

## 6. 图标映射表

| BUI 图标（源包） | 落点 | lucide-react 等效 |
|---|---|---|
| `IconHome` / `IconEditBig` / `IconPlusMedium` / `IconUserAdd` / `IconSettingsGear1` / `IconChevronDownSmall` / `IconCrossSmall` / `IconMagnifyingGlass` / `IconCheckmark1Small` / `IconArrowBoxLeft` / `IconSidebarLeftArrow` / `IconPopsicle2`（@central-icons-react） | sidebar-nav | `Home` / `SquarePen` / `Plus` / `UserPlus` / `Settings` / `ChevronDown` / `X` / `Search` / `Check` / `LogOut` / `PanelLeft` / `IceCream`（或品牌 logo 占位，lucide 无 Popsicle） |
| `ArrowUp` / `ChatBubbleQuestion` / `Check` / `EmojiSatisfied` / `NavArrowRight` / `Refresh` / `Scissor` / `Spark` / `TextBox` / `Xmark`（iconoir-react） | selection-actions | `ArrowUp` / `MessageCircleQuestion` / `Check` / `Smile` / `ChevronRight` / `RotateCw` / `Scissors` / `Sparkles` / `TextCursorInput` / `X` |

> Popsicle / 部分品牌图标无精确对应时，用品牌 logo 或通用图标占位，统一以 `lucide-react` 收口，避免多套图标依赖。

---

## 7. 文档修改清单（已随本方案执行）

- **`06_前端开发指南.md`**：§1 技术栈追加 Beautiful UI / `recharts` / `lucide-react`；§2 目录追加 `src/components/bui/`；**新增专章「设计系统：Beautiful UI」**（令牌/类/依赖替换/暗色/全局 CSS/图标映射/20 件映射）；原 §10「样式与主题」顺延为 §11。
- **`AGENT.md`**：§1 技术栈补 Beautiful UI / recharts / lucide-react；§2 文档地图补本文件与 06 设计系统章。
- **`GraphRAG_系统架构文档.md`**：决策记录 **J24**（前端设计系统）；第一章前端技术栈补 Beautiful UI 说明。
- **`README.md`**：文档清单补本文件。
- **`01_开发流程.md`**：§2 技术栈补 Beautiful UI / recharts / lucide-react；补 BUI 接入说明一句。

---

## 8. 风险与注意

- 令牌**数值为初版反推**，组件可编译；像素级保真需对照 beautifului.dev 目视微调。
- `records-*`/`sidebar-*` 内部实现复杂，先给可用骨架再收敛。
- `recharts` 改造需重写 insight-cards 的图表部分（Liveline 的 `series`/`window`/`scrub`/`pulse`/`momentum` 等属性无直接对应，用 recharts `LineChart`/`BarChart` + `Tooltip` 替代）。
- 图标映射需建 `@central-icons-react`→lucide 与 `iconoir-react`→lucide 两张对照表（Popsicle 等无对应图标用品牌占位）。

---

## 9. 执行任务清单

1. 重建 `globals.css`（令牌+类+keyframes，含 `.dark` 覆盖）
2. 创建 `src/components/bui/` 并落地零依赖组件（批次1）
3. 落地含 `GlideMenu` 组件（→shadcn `Popover`/`DropdownMenu`，批次2）
4. 落地 `chat-composer`/`prompt-bar`（剥离 `posthog-js`/`glimm`，批次3）
5. `insight-cards` 改造为 `recharts`（批次4）
6. `flowchart` / `selection-actions` / `records-table` 适配（批次5，含图标映射与 `StreamText`/`Shimmer` 本地化）
7. 业务层包装：接 `chatStore`/`sessionStore`/`types/api.ts`
8. 改写文档：06/AGENT.md/架构 J24/README/01（本文件 §7 已覆盖）

---

*变更记录：v1.0（2026-08-23）依 J24 创建前端设计系统（Beautiful UI）落地方案。*
